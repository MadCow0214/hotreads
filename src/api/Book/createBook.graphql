type CreateBookResult {
  error: Int
  book: Book
}

type Mutation {
  createBook(
    title: String!
    subtitle: String
    category: Int!
    authorId: String!
    company: String!
    image: String
    desc: String
    publishDate: String
  ): Book!
}
