type User {
  id: ID!
  nickName: String!
  firstName: String
  lastName: String
  email: String!
  avatar: String
  uploadedBooks: [Book!]!
  wantedBooks: [Book!]!
  reviews: [Review!]!
}

type Book {
  id: ID!
  title: String!
  subTitle: String!
  category: Int!
  author: String!
  company: String
  image: String
  desc: String
  publishDate: String!
  createdAt: String!
  uploader: User!
  wantedUsers: [User!]!
  tags: [Tag!]!
  reviews: [Review!]!
}

type Review {
  id: ID!
  user: User!
  book: Book!
  text: String
  star: Int!
  createdAt: String!
}

type Tag {
  id: ID!
  name: String!
  books: [Book!]!
}

type Author {
  id: ID!
  name: String!
  books: [Book!]!
}
