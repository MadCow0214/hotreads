type User {
  id: ID!
  nickName: String!
  firstName: String
  lastName: String
  email: String!
  avatar: String
  uploadedBooks: [Book!]!
  wantedBooks: [Book!]!
  reviews: [Review!]!
}

type Book {
  id: ID!
  title: String!
  subTitle: String!
  category: Int!
  author: Author
  company: String
  image: String
  desc: String
  publishDate: String!
  createdAt: String!
  uploader: User!
  wantedUsers: [User!]!
  reviews: [Review!]!

  # Computed Fields
  wantedUserCount: Int!
}

type Review {
  id: ID!
  user: User!
  book: Book!
  text: String
  star: Int!
  createdAt: String!
}

type Author {
  id: ID!
  name: String!
  desc: String
  books: [Book!]!
}
